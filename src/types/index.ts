export type TInputs = {
  amount: string;
};

export type TModalInvoices = {
  client: string;
  paymentStatus: string;
  cargoStatus: string;
  date: string;
  amount: number;
  typeOfPayment: string;
  installmentsPayed: string;
  paymentDueDate: string;
}
